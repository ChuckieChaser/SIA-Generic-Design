const showModal = ({ icon, heading }) => {};

// *** HOW TO CALL *** //
const successButton = $('#successButton');
successButton.on('click', () => alert('wait lng po ginagawa pa'));

const warningButton = $('#warningButton');
warningButton.on('click', () => alert('wait lng po ginagawa pa'));

const errorButton = $('#errorButton');
errorButton.on('click', () => alert('wait lng po ginagawa pa'));
